======================================================
梯度下降作业(代码)：
  目标函数：
    y = x**2 + b * x + c
  需求：求解最小值对应的x和y(这里的最小值指的是函数y在所有数据上的一个累计最小值)
  要去：写代码
    数据：
		b: 服从均值为-1，方差为10的随机数
		c：服从均值为0，方差为1的随机数
	假定b、c这样的数据组合总共1、2、10、100、10w、100w条数据,求解在现在的数据情况下，目标函数的取最小值的时候，x和y分别对应多少？
  周二晚上讲

b=-1.0 c=0.0 -> y1=x**2 - x
b=-0.5 c=0.5 -> y2=x**2 - 0.5*x +0.5
也就是要求Y = y1 + y2 最小化

